#include "main.h"
#include<string.h>
#include<stdio.h>
/**
 * _strcpy - write array
 * @dest: copy destiny
 * @src: origin
 * Return: (dest) char
 */
char *_strcpy(char *dest, char *src)
{
	strcpy(dest, src);
	return (dest);
}
