#include "main.h"
#include<string.h>
/**
 * _strlen - count letters
 * @s: string to evaluate
 * Return: (count) success
 */
int _strlen(char *s)
{
	int count;

	count = strlen(s);
	return (count);
}
